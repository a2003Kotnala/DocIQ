#!/usr/bin/env bash
set -euo pipefail

echo "DocIQ uses provider-specific models. In local development, deterministic adapters are active by default."
echo "Install PaddleOCR, PP-Structure, and model assets here for production-like execution."

